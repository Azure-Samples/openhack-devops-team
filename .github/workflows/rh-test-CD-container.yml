name: RH CD Test

on:
  push:
    branches: 
    - unittest-rh

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Echo Test
      run: echo 'Build container'